# Enable persistence to save messages and subscriptions
persistence true
persistence_location /mosquitto/data/

# Default MQTT listener
listener 1883

# Enable anonymous access (for simple testing)
allow_anonymous true

# Optional: Add a listener for WebSockets
listener 9001
protocol websockets